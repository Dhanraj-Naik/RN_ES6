import React from 'react';
import { Text, View } from 'react-native';

const Rest = ({
    params,
}) => {
    return (
        <View>
            <Text>Rest</Text>
        </View>
    );
};

export default Rest;
